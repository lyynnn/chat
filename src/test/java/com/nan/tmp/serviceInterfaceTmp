package com.nan.service.[objName];

import java.util.List;

import com.nan.pojo.[ClassName];

public interface I[objName]Service {
	List<[ClassName]> get[ClassName]s([ClassName] [objName]);
	[ClassName] get[ClassName]ById(String id);
	int add([ClassName] [objName]);
	int update([ClassName] [objName]);
	int del(String id);
	int count([ClassName] [objName]);
}